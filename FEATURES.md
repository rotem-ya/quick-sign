# QuickSign — צ'קליסט

מסמך המעקב היחיד של הפרויקט. מעודכן בכל סבב.

- ✅ ממומש, נבדק ופרוס
- 🔜 בתור לפיתוח — אפשר לממש מכאן, בלי תלות חיצונית
- 🔑 ממתין לבעל המוצר — דורש חשבון/מפתח/החלטה שלך

---

## ✅ ממומש

### עיצוב מחדש — כל האפליקציה מאפס (סבב נוכחי)
- ✅ **מערכת עיצוב חדשה מקצה לקצה**, כי העיצוב הקודם (למרות ה-handoff
  שיושם בעבר רק במסך העבודה) נראה גנרי — טפסים עם מסגרות שחורות דקות,
  רקע בז'-אפרפר מעומעם, כרטיסים שטוחים בלי היררכיה, וכפתור "שליחה" במצב
  לא-פעיל שנראה שבור (אפור מת). נבנה מאפס:
  - **פלטת ניטרלים קרירה** (`lib/theme/design_tokens.dart`) במקום בז' —
    כחול המותג (`#3E5EE0`) נשאר הצבע הרווי היחיד על המסך, כל השאר גווני
    אפור-כחלחל. סקאלת מרווחים/רדיוסים/צללים אחידה (`space*`/`radius*`/
    `shadow*`) כך שכל מסך משתמש באותם ערכים בדיוק.
  - **`ThemeData` מלא ב-`app.dart`**: `ColorScheme` מותאם (לא ברירת המחדל
    הסגלגלה של `ColorScheme.fromSeed`), `CardTheme` עם צל עדין במקום
    מסגרת קשה, `InputDecorationTheme` ממולא-וללא-מסגרת עם focus כחול,
    `FilledButton`/`OutlinedButton`/`TextButton`/`Chip`/`Dialog`/
    `BottomSheet`/`SnackBar` — כולם עם רדיוסים ואופי אחיד. המסכים
    המשניים (מעצב חותמת, הגדרת חותמת, ניהול עמודים, גיליונות חתימה/הערה)
    כמעט כולם עוצבו מחדש **דרך ה-theme בלבד**, בלי לגעת בכל widget בנפרד.
  - **מסך העבודה**: header עם צל עדין במקום קו הפרדה שטוח, מצב ריק
    (empty state) עם עיגול גרדיאנט דו-שכבתי וכפתור "פתיחת מסמך" בעיצוב
    גרדיאנט תואם לכפתור השליחה, רמזים (שיתוף/גרירה) כגלולות רכות במקום
    טקסט חופשי מרחף.
  - **סרגל כלים תחתון**: שלושת הכלים (חתימה/חותמת/טקסט) עכשיו בתוך "בקרת
    מקטע" (segmented control) עם רקע אפרפר ופריט נבחר בכרטיס לבן+צל —
    כמו iOS, לא שלושה כפתורים צפים בנפרד. כפתור "שליחה ושמירה" במצב
    לא-פעיל עבר מאפור מת לגוון כחול-בהיר של המותג, כדי שלא ייראה שבור.
  - **הגדרות**: כותרות מקטע קיבלו "צ'יפ" אייקון צבעוני; "פריט חדש" (חתימה/
    חותמת/קומבו) עבר מכפתור מסגרת ריק לכרטיס מוזמין עם רקע כחול-בהיר
    ואייקון עיגול; כרטיסי פריט שמורים קיבלו צל+מסגרת כחולה כשהם ברירת
    מחדל, במקום מסגרת אפורה שטוחה.
  - **פאנל הכלים (web)**, **היסטוריה**, **ניהול עמודים**: אותה שפה —
    "בקרת מקטע" ללשוניות, תמונות ממוזערות עם צל במקום מסגרת, מצבי ריק
    עם עיגול-אייקון גרדיאנט.
  - אומת קצה-לקצה בדפדפן אמיתי (build web + Playwright): מצב ריק, מסמך
    פתוח עם overlay/בקרת זום, סרגל כלים, פאנל הגדרות (כולל בלי מסמך
    פתוח), מסך ניהול עמודים.

### קבלת מסמכים
- ✅ שיתוף (Share) מוואטסאפ/מייל — PDF ותמונות
- ✅ **גרירת קובץ לאתר (web)** — שחרור בכל מקום בעמוד פותח את המסמך, עם
  חיווי חזותי בזמן הגרירה, בסגנון כל קורא PDF/עורך מסמכים בדפדפן
- ✅ "פתיחה באמצעות" (Open with) — כולל בזמן שהאפליקציה רצה
- ✅ בחירת קובץ ידנית (מובייל + web)
- ✅ תמונה נעטפת אוטומטית כעמוד PDF

### חתימה, חותמת וטקסט
- ✅ הנחה בלחיצה ארוכה + גרירה לסימון האזור והגודל
- ✅ גודל ברירת מחדל קבוע וסטנדרטי (חתימה ~4 ס"מ על A4), ניתן לעריכה
- ✅ חתימה עם חותמת או בלי חותמת — בחירה בין חתימה בודדת לפריט "קומבו"
  (חתימה+חותמת ששולבו ונשמרו יחד מראש, ראו למטה)
- ✅ **הנחה על המסמך מוגבלת לספרייה בלבד**: לחיצה ארוכה על הדף פותחת
  בורר של הפריטים השמורים (או משתמשת ישירות בברירת המחדל) — לעולם לא
  פותחת מצלמה/גלריה/קנבס ציור באמצע העבודה על מסמך. הכנת פריט חדש
  (צילום, ציור, עיצוב) קורית רק במסך ההגדרות
- ✅ חתימה/חותמת על כל החוברת במיקום אחד — **ועריכה קבוצתית**: הזזה,
  שינוי גודל, סיבוב או מחיקה של אחת מעדכנת את כולן (עם ביטול)
- ✅ עריכת פריט: גרירה, ידית גרירה לשינוי גודל + צביטה, סיבוב (ידית 15° +
  שתי אצבעות), מחיקה עם ביטול, עריכת טקסט שהונח
- ✅ טקסט חופשי עם צ'יפים מהירים (תאריך / אושר / התקבל / שם מהפרופיל)

### חותמת
- ✅ צילום / גלריה עם סימון אזור (חיתוך בפינות נגררות)
- ✅ הסרת רקע אדפטיבית — גם דף מצולם אפרפר/מוצל נעלם, רק הדיו נשאר
- ✅ מעצב חותמת דיגיטלית: תבנית ערוכה — שם + 2 שורות, 4 צבעי דיו,
  מלבן/אליפסה, **בחירת מסגרת: ללא / יחידה / כפולה**, תצוגה חיה על רקע
  משובצת-שקיפות (לא לבן, לא שום צבע) שמוכיחה שאין רקע — גם בתצוגות
  המקדימות של חותמת שמורה בהגדרות ובמסך הגדרת החותמת

### ספריית חתימות וחותמות
- ✅ **כמה חתימות וכמה חותמות שמורות**, לא רק אחת מכל סוג — כל פריט עם
  שם משלו (ניתן לעריכה), תצוגה מקדימה על רקע משובצת-שקיפות
- ✅ הוספה, שכתוב שם, מחיקה עם ביטול — לכל פריט בנפרד, במסך ההגדרות
- ✅ **עריכה אמיתית**: חותמת שנוצרה במעצב נפתחת חזרה עם כל הפרטים (שורות
  הטקסט, צבע, צורה, מסגרת) לעריכה נוספת — לא רק החלפה מאפס. חותמת
  מצולמת: "צילום מחדש" (חיתוך/הסרת רקע מחדש). חתימה: "ציור מחדש"
- ✅ **שילובי חתימה+חותמת ("קומבו")**: בוחרים חתימה שמורה וחותמת שמורה,
  הן משולבות ונשמרות כפריט אחד עם שם משלו — בדיוק כמו חתימות וחותמות
  רגילות (עריכה = בחירה מחדש של הזוג, מחיקה עם ביטול)
- ✅ **ברירת מחדל** — לכל אחד משלושת הסוגים (חתימה / חותמת / קומבו) אפשר
  לסמן פריט ככוכב-ברירת-מחדל. לחיצה על הדף עם ברירת מחדל מגדירה מניחה
  אותה מיד, בלי לפתוח בורר; בלי ברירת מחדל — נפתח הבורר המוגבל לספרייה
  (ראו למעלה)
- ✅ בעת הנחת חתימה על מסמך, עם כמה שמורות ובלי ברירת מחדל — בורר יחיד
  שמציג גם חתימות בודדות וגם קומבואים, לבחירה בין "עם חותמת" ל"בלי"
  בכל הנחה
- ✅ הגירה אוטומטית וחד-פעמית של החתימה/החותמת הישנות (מהגרסה הקודמת,
  שתמכה בפריט אחד בלבד) לתוך הספריה החדשה — שום דבר לא אבד

### ארגז כלים (web)
- ✅ **פאנל צד קבוע בדפדפן** — אייקון יחיד בכותרת (מחליף את אייקוני היסטוריה
  והגדרות הנפרדים, רק ב-web) פותח/סוגר פאנל שממוקם לצד המסמך (לא מכסה
  אותו) עם ההגדרות המלאות — כולל ספריית החתימות/חותמות, חשבון, תיקיית
  ברירת מחדל וגיבוי — בלי לנווט הרחק מהמסמך. עריכה מפורטת (למשל ציור
  חתימה מחדש) עדיין נפתחת כמסך מלא מעל הכל, וחוזרים לפאנל אחריה.
- ✅ **היסטוריית מסמכים חתומים עובדת עכשיו גם ב-web** — הפאנל מציג שני
  טאבים (הגדרות + היסטוריה). האחסון עצמו (קבצי ה-PDF החתומים) עבר
  ל-IndexedDB בדפדפן במקום קבצים במערכת הקבצים (שלא קיימת ב-web); רשימת
  הפריטים (שם/תאריך/גודל) ממשיכה להישמר ב-localStorage כמו קודם. נבדק
  קצה-לקצה: חתימה על מסמך → שליחה → הפריט מופיע בהיסטוריה → מחיקה עם
  ביטול → שחזור מוצלח

### מסמך ותצוגה
- ✅ **מסך מלא אימרסיבי** — כשמסמך נפתח, סרגל הכלים העליון והתחתון נעלמים
  מיד (גם ה-status/nav bar של המערכת נעלמים באנדרואיד/iOS), והמסמך תופס
  את כל המסך. לחיצה קצרה (לא לחיצה ארוכה שמסמנת אזור) על הדף מציגה/
  מסתירה אותם בהנפשה חלקה, בדיוק כמו קורא PDF מקצועי
- ✅ זום עמוק פי 3 (עד 18x, היה 6x): צביטה + כפתורי ± על המסך + Ctrl/Cmd+
  גלגלת עכבר (web)
- ✅ **זום-אאוט מתחת ל-100%** (עד 0.3x) — לראות מסמך רב-עמודים בשלמותו או
  כמה עמודים בבת אחת. תוקן תוך כדי באג ישן וסמוי בקוד הזום: הפונקציה
  הסטנדרטית של Flutter לקריאת רמת הזום מהמטריצה (`getMaxScaleOnAxis`)
  בודקת גם את ציר ה-Z (שאף פעם לא משתנה אצלנו, ונשאר תמיד 1) — כך שכל
  זום מתחת ל-100% הוקרא בטעות בחזרה כ-100% בדיוק, מה שתקע את כפתור
  ה"−" אחרי לחיצה אחת בלבד. לא נתפס קודם כי לפני הבקשה הזו זום מתחת ל-1
  פשוט לא היה אפשרי. תוקן עם פונקציית עזר ייעודית שקוראת רק ציר X/Y
- ✅ **אחוז זום גלוי + צעד גדול יותר** — מספר האחוזים (למשל "160%") מוצג
  עכשיו בין כפתורי ±, ומתעדכן חי; כל לחיצה קופצת פי 1.6 במקום פי 1.4 —
  זום מורגש ומיידי יותר, לא רק טכנית עמוק יותר
- ✅ **גלילה בגלגלת העכבר (web)** — גלגלת רגילה גוללת את המסמך למעלה/למטה
  כמו בכל דפדפן/קורא PDF (Foxit ודומיו), במקום להגדיל/להקטין; Ctrl/Cmd+
  גלגלת ממשיך לשמש לזום, עם סמן זכוכית מגדלת בזמן שהחזקתם Ctrl. מוגבל אך
  ורק למסך העבודה כשהוא גלוי בפועל, כדי לא לפגוע בגלילה הרגילה במסכי
  הגדרות/היסטוריה/דיאלוגים
- ✅ **הזזה עם לחצן גלגלת העכבר (web)** — סמן כף-יד בזמן החזקת הלחצן
  האמצעי, לתזוזה חופשית של המסמך
- ✅ אינדיקטור עמוד נוכחי (2/33)
- ✅ **ניהול עמודים** (כפתור ייעודי בראש המסך): מחיקת עמודים קיימים
  (סימון כמה שרוצים, עם אישור אם יש חתימות/חותמות שייעלמו), **סידור מחדש
  של עמודים בגרירה** (לחיצה ארוכה וגרירה של תמונה ממוזערת למקום חדש),
  הוספת עמוד ריק, הוספת עמודים מתמונות (כמה בבת אחת — תמונה→PDF), וצירוף
  כל העמודים מקובץ PDF אחר לסוף המסמך. הכול נשמר וקטורי (לא רסטריזציה) עד
  לייצוא הסופי; חתימות/חותמות קיימות זזות אוטומטית לעמוד הנכון אחרי מחיקה
  או סידור מחדש
- ✅ גודל טקסט-הערה פרופורציונלי לטקסט של המסמך
- ✅ **רינדור עצל (lazy)** עם placeholder פועם (shimmer) — במסמכים גדולים
  (למשל תוכניות הנדסיות של 50+ עמודים) רק העמודים בתוך אזור התצוגה +
  חוצץ קטן נטענים בפועל; שאר העמודים מציגים פעימה עדינה במקום ריבוע אפור
  שטוח, עד שגוללים אליהם. פותר טעינה איטית בקבצים
  גדולים במקום לטעון את כל העמודים בבת אחת בפתיחה
- ✅ **"שמירה" בלי חתימה** — לחיצה על "שליחה" במסמך שעדיין לא נחתם פותחת
  ישירות את תפריט השיתוף/שמירה (כולל שמירה מהירה לתיקיית ברירת המחדל)
  על הקובץ כפי שהוא — לא רק לאחר חתימה. זה גם ה"כפתור שמירה/שמור בשם"

### ייצוא — משוטח ובלתי-הפיך
- ✅ רסטריזציה מלאה: החתימה חלק מפיקסלי הדף — אין העתקה/סימון/חילוץ
- ✅ דיאלוג אזהרה "הטמעה לצמיתות" לפני ייצוא
- ✅ שיתוף · שמירה אל… (Drive/OneDrive/תיקייה משותפת דרך המערכת) ·
  הדפסה · הורדה בדפדפן

### פלטפורמות והפצה
- ✅ Android — APK עדכני ב-`releases/quick-sign-arm64.apk`
- ✅ Web — חי ב-**https://rotem-ya.github.io/quick-sign/**, מתעדכן
  אוטומטית מכל מיזוג ל-main
- ✅ תשתית Firebase Hosting מוכנה בריפו (למעבר עתידי + דומיין משלך)

### היסטוריית מסמכים
- ✅ **כל מסמך שנחתם נשמר לצמיתות** במסך "היסטוריה" חדש (אייקון בראש
  המסך) — עותק קבוע בתיקיית האפליקציה, נפרד מהקובץ הזמני של שיתוף/הדפסה.
  לא נמחק אוטומטית לעולם — **רק המשתמש מוחק**, עם "ביטול" זמין לכמה שניות
  אחרי מחיקה. מכל פריט בהיסטוריה: **פתיחה לצפייה** (טוען את המסמך חזרה
  למסך העבודה — אינטראקטיבי, לא רק תצוגה סטטית) / שיתוף / שמירה מהירה
  לתיקיית ברירת המחדל / שמירה אל… / הדפסה — אותו תפריט כמו במסך העבודה.
  אנדרואיד/iOS בלבד (כמו תיקיית ברירת המחדל — דורש קבצים מקומיים).

### פרופיל, גיבוי ופרטיות
- ✅ הכול מקומי — אין שרת, המסמכים לא עוזבים את המכשיר
- ✅ פרופיל מקומי (שם), מסך הגדרות עם ניהול חתימה/חותמת
- ✅ חתימות שורדות מחיקה (Android Auto Backup)
- ✅ **גיבוי/שחזור לקובץ נייד** — עובר בין מכשירים בלי חשבון. הקובץ כולל
  את **כל** הספרייה (כל החתימות, החותמות, הקומבואים, וברירות המחדל) —
  לא רק פריט אחד מכל סוג. שחזור תמיד יוצר עותקים טריים (לא דורס לפי מזהה),
  כך שאין התנגשות גם אם משחזרים לאותו מכשיר פעמיים, וגם קובצי גיבוי ישנים
  (מהגרסה הקודמת, עם חתימה/חותמת אחת בלבד) עדיין נטענים נכון
- ✅ **גיבוי מהיר לתיקיית ברירת המחדל** (Android) — כשכבר נבחרה תיקייה
  (Drive/OneDrive/Dropbox דרך SAF), כפתור אחד כותב את הגיבוי ישירות
  לתיקייה — בלי לפתוח דיאלוג "שמירה בשם". זו הדרך ש"החתימות שלי מגובות
  בענן" עובדת כרגע **בלי כל חשבון/OAuth** — לעומת "התחברות" אמיתית עם
  Google/Apple, שדורשת פרטי מפתח מבעל המוצר (ראו 🔑 למטה)
- ✅ **אישור חזותי במספרים**: שמירה/שחזור של גיבוי מציגים כמה פריטים
  בפועל נשמרו/נוספו ("הגיבוי נשמר — 3 פריטים"), לא רק "בוצע בהצלחה" גנרי
  שנראה אותו דבר גם כשלא קרה כלום
- ✅ **סעיף "חשבון" גלוי** במסך ההגדרות (מיד אחרי הפרופיל): "התחברות עם
  Google" מציג בבירור שזה עדיין לא זמין ומסביר בדיוק מה חסר (מפתח OAuth
  שרק בעל המוצר יכול ליצור) — לא כפתור שנראה שעובד אבל בפועל לא

### עיצוב ונגישות
- ✅ Material 3 + מצב כהה, גופן Heebo אחיד, אייקון אפליקציה
- ✅ מטרות מגע ≥48dp, תוויות קורא-מסך על כל הידיות, רטט עדין
- ✅ באנר פרסומות בתחתית (מזהי בדיקה)

### תיקיית ברירת מחדל — "חיבור" ל-Drive/OneDrive בלי OAuth
- ✅ בהגדרות: בחירת תיקייה פעם אחת דרך בורר המערכת (SAF). Drive, OneDrive,
  Dropbox וכל אפליקציית אחסון נרשמות בעצמן כספק-תיקיות למערכת — הבורר
  מציג אותן בדיוק כמו תיקייה מקומית. ברגע שנבחרה תיקייה, ההרשאה נשמרת
  לצמיתות (persistable), ובמסך השליחה מופיע כפתור "שמירה מהירה" שכותב
  ישירות לתיקייה שנבחרה, בלי לפתוח דיאלוג בכלל.
- זה **לא** גישה מלאה לחשבון (אין רשימת קבצים, אין ניווט בתוך Drive מתוך
  האפליקציה) — זו שמירה ישירה לתיקייה קבועה. לגישת חשבון מלאה ראו 🔑 למטה.

### ספריית תיקיות — עיון וסינון בקבצים
- ✅ **מסך "ספריית תיקיות" חדש** — ניתן להוסיף כמה תיקיות שרוצים (כולל
  תיקיות בתוך Drive/OneDrive/Dropbox, דרך אותו מנגנון SAF כמו תיקיית
  ברירת המחדל), והמסך מציג רשימה אחת עם כל הקבצים הנתמכים (PDF/תמונות)
  שבכל התיקיות יחד. **סינון** לפי סוג (הכול / PDF / תמונות) ו**מיון**
  (שם / תאריך / גודל, עם היפוך כיוון בלחיצה חוזרת על אותו מיון). לחיצה
  על קובץ פותחת אותו ישירות למסך העבודה, כמו "פתיחה לצפייה" בהיסטוריה.
  נגיש מאייקון תיקייה חדש בראש המסך (מובייל/web צר) ומטאב שלישי בארגז
  הכלים (web רחב, לצד הגדרות והיסטוריה).
- ✅ **גם ב-web** — דרך File System Access API של הדפדפן (כרום/אדג')
  כשנתמך; תיקיות שנבחרו נשארות זמינות לאורך הסשן הנוכחי (לא נשמרות בין
  טעינות דף, בגלל שהדפדפן ממילא דורש אישור הרשאה מחדש בכל טעינה) —
  בדפדפנים שלא תומכים (פיירפוקס/ספארי) המסך מציג הודעה ברורה במקום
  להתנהג כאילו זה נתקע.
- 🐛→✅ **טקסט על כפתורים ראשיים (FilledButton) לא הוצג בכלל בדפדפן** —
  התגלה תוך כדי בניית המסך הזה: כל כפתור "אישור/שמירה/הוספה" שהשתמש
  ב-`FilledButton` הציג מלבן צבעוני ריק לגמרי, בלי הטקסט (גם באייקון
  לבד לפעמים כן, רק הטקסט נעלם) — כולל בדיאלוג "עדיין לא זמין" של
  ההתחברות, ובכפתורי אישור בכמה גיליונות תחתונים. הצטמצם ל-override
  של `textStyle` בהגדרת התימה (`FilledButtonThemeData`/עכשיו
  `ElevatedButtonThemeData`) שמתנגש עם צביעת הטקסט האוטומטית — הוסר
  ה-override, וכל האתר עבר מ-`FilledButton` ל-`ElevatedButton` בעיצוב
  שטוח זהה (בלי צל), כך שהטקסט תמיד מוצג. באג ותיק, לא קשור לפיצ'ר
  החדש — תוקן בכל המסכים בבת אחת.

---

## 🔜 בתור לפיתוח

- 🔜 צילום חותמת → חותמת דיגיטלית ערוכה — OCR מקומי משחזר את הטקסט
  במעצב עם אותו מספר שורות וסדר (הפונט: Heebo, הקרוב הזמין)
- 🔜 **התאמת גודל אוטומטית לפי קנה המידה של הגיליון** — במקום שבר קבוע
  מרוחב הדף, החתימה/החותמת/הקומבו יתבססו על גודל הכתב הנמדד בפועל
  במסמך (כבר קיים מנגנון דומה להערות טקסט) — כך שהגודל האוטומטי ישקף
  את קנה המידה האמיתי של הגיליון (דף גדול עם כתב גדול = חתימה גדולה
  יותר בפועל, לא רק שבר קבוע מרוחב הדף). שינוי גודל ידני נשאר זמין תמיד.
- 🔜 **מראה "אמיתי" לחותמות בלבד** — כל הנחה של חותמת (לא חתימה) תקבל
  זווית סיבוב אקראית קלה (כמו הטבעה ביד, לא ישר-מדויק דיגיטלי), ומריחות
  דיו קלות מאוד ואקראיות ליד קצה ההטבעה — כל הנחה שונה מהקודמת, כמו
  חותמת אמיתית שמוטבעת כמה פעמים.

---

## תיקוני באגים (מהסבב האחרון)

- 🐛→✅ **גיליונות תחתונים נחתכים מתחת לפקדי המערכת של אנדרואיד** — כמה
  גיליונות (בורר הפריט השמור בעת הנחה, ציור חתימה, הערת טקסט) לא היו
  עטופים ב-`SafeArea`, אז על מכשיר עם ניווט במחוות (או שורת ניווט
  תלת-כפתורית) התוכן בפועל (הפריטים לבחירה, כפתורי אישור) נכנס מתחת
  לפקדי המערכת ונחתך/לא נגיש — נראה כאילו "המערכת מסתירה" את האפליקציה.
  תוקן: `SafeArea(top: false)` בכל הגיליונות התחתונים
  (`signature_sheet.dart`, `note_sheet.dart`) — הגיליון הצדדי כבר היה
  תקין קודם.
- ✅ **חדש: חתימה ידנית מיידית כשאין חתימה שמורה** — עד עכשיו, לחיצה
  ארוכה לחתימה כשאין אף חתימה שמורה רק כיוונה להגדרות. עכשיו: אם אין
  חתימה שמורה (ואין ברירת מחדל), נפתח קנבס ציור **ישירות במקום ההנחה**,
  החתימה מונחת מיד אחרי האישור, ומוצע ב-snackbar "לשמור את החתימה הזו
  לפעם הבאה?" — בלי לעזוב את המסמך. חותמת נשארת מכוונת להגדרות (דורשת
  צילום/עיצוב, לא ציור מיידי).
- ✅ **חדש: מסגרת מיקום נגררת בלחיצה ארוכה** — לחיצה ארוכה כבר הציגה
  מסגרת זמנית, אבל היא הייתה קבועה במקום הלחיצה ורק שינתה גודל לפי
  מרחק הגרירה. עכשיו המסגרת מופיעה בגודל הסטנדרטי של הכלי הפעיל **ועוקבת
  אחרי האצבע** תוך כדי גרירה (מיקום, לא שינוי גודל) — כך שאפשר לכוון את
  המיקום המדויק *לפני* שהפריט בכלל מונח, ולא רק אחרי (שגם ממשיך לעבוד
  כרגיל — גרירה/שינוי גודל/סיבוב על הפריט שכבר הונח). נבדק ב-Playwright:
  המסגרת עוקבת אחרי הגרירה בלי לשנות גודל.
- ✅ **חדש: פתיחה לצפייה מההיסטוריה** — לחיצה על פריט בהיסטוריה פתחה עד
  עכשיו רק תפריט שיתוף/שמירה/הדפסה, בלי דרך לראות בפועל את המסמך. נוסף
  "פתיחה לצפייה" כפעולה ראשונה בתפריט (`lib/widgets/action_sheet.dart`
  — רכיב משותף חדש, גם למסך העבודה וגם להיסטוריה, במקום שני תפריטים
  כפולים) — טוען את בייטים המסמך בחזרה למסך העבודה, לגמרי אינטראקטיבי
  (זום, ואם עדיין לא נחתם/שוטח — גם המשך עריכה).
- 🐛→✅ **רינדור עמוד יכול להיתקע לצמיתות ב-web, בלי שגיאה** — אותרה תוך
  כדי בדיקת "פתיחה לצפייה": פתיחת מסמך מבייטים שחזרו מ-IndexedDB (היסטוריה
  ב-web) מיד אחרי שמסמך אחר כבר עבר רינדור באותו טעינת-עמוד, תקעה את
  קריאת הרינדור של pdf.js לצמיתות — לא שגיאה, סתם שקט, ספינר שלא נעלם
  לעולם. נבדק ונשלל: לא שחיתות נתונים (אומת גם עם שכפול בייטים מפורש וגם
  עם אחסון כ-base64 טקסטואלי במקום TypedArray גולמי — `history_store_
  web.dart`), ולא שיתוף buffer/pipeline (אומת עם pipeline רינדור טרי
  לגמרי לכל פתיחה) — רק **טעינה מחדש מלאה של הדף** (worker חדש לגמרי)
  פתרה את זה בבדיקות, מה שמצביע על תקלת state פנימית בעובד ה-web של
  pdf.js/pdfx שלא בשליטתנו. **תוקן בהגנה, לא בשורש**: לכל רינדור עמוד יש
  עכשיו תקרת זמן (12 שניות, `PdfRenderService.renderTimeout`), ובכשלון —
  כרטיס "לא נטען — לנסות שוב" עם לחיצה לניסיון חוזר, במקום ספינר תקוע
  לצמיתות. **חשוב**: זו תופעה שנצפתה רק בדפדפן (Playwright) — האפליקציה
  הנייטיבית (Android/iOS) משתמשת ברינדור pdfium דרך platform channel,
  בלי Web Worker בכלל, וכנראה לא חשופה לבאג הזה — כדאי לוודא בפועל
  שהצפייה מההיסטוריה עובדת חלק גם באפליקציה, לא רק בדפדפן.
- 🐛→✅ **רזולוציית רינדור נמוכה מדי בגליונות בפורמט גדול** — הדפים מרונדרים
  פעם אחת מראש (`pdf_render_service.dart`), ותקרת הרוחב הקודמת (2048px)
  הייתה למעשה **נמוכה מהרזולוציה הטבעית** של דף גדול (למשל גיליון הנדסי
  בפורמט ANSI-D, ~2448pt רוחב ב-72dpi) — כך שגם בלי שום זום המסמך כבר
  הוצג ברזולוציה חלקית, וזום עמוק (עכשיו עד 3000%) רק הגדיל את זה, נראה
  מפוקסל. תוקן: תקרת הרוחב עלתה ל-4096px, וקנה המידה הבסיסי (למסמכים עד
  12 עמודים) מ-×2 ל-×3 — מסמכי A4/Letter רגילים (רובם קטנים בהרבה מהתקרה)
  לא מושפעים בזיכרון, גיליונות גדולים מקבלים עד פי 4 יותר פיקסלים.
- 🐛→✅ **תפריט השליחה/שיתוף נראה גנרי** — הוחלף מרשימת `ListTile` שטוחה
  לכרטיס אלגנטי עם ידית-גרירה, צ'יפ-אייקון עגול לכל פעולה, וטיפוגרפיה
  נקייה — בהשראת תפריט השיתוף של Samsung Notes שהמשתמש ציין כדוגמה.
- 🐛→✅ **פאנל ההגדרות נחתך בדפדפן נייד** — `ToolboxPanel` (הפאנל הצדדי
  ב-web) רוחבו קבוע 380px, מיועד לחלון דסקטופ רחב לצד המסמך — על מסך נייד
  צר (אפילו לפני הסבב הזה, אבל התגלה רק עכשיו) הפאנל לא נכנס לרוחב הזמין,
  והתוכן (בעברית, RTL) נחתך בקצה. תוקן: מתחת לרוחב `760px` הדפדפן חוזר
  לאותה ניווט מסך-מלא ל-הגדרות/היסטוריה שהאפליקציה הנייטיבית כבר משתמשת
  בו תמיד (`Navigator.push`) — בלי שום מגבלת רוחב. הפאנל הצדדי הקבוע נשאר
  רק מ-760px ומעלה (חלון דסקטופ אמיתי). נבדק ב-Playwright עם locale
  עברי ב-viewport של 390px — נפתח מסך הגדרות מלא, בלי חיתוך.
- 🐛→✅ **כפתור "פתיחת מסמך" מוסתר מתחת לגלילה במסך ריק, על מכשיר אמיתי** —
  נתגלה מיד אחרי העיצוב מחדש: על מכשיר Android אמיתי (עם באנר פרסומת אמיתי
  + מסגרת המערכת) גובה התוכן של המצב הריק (עיגול+כותרת+כפתור+רמזים) היה
  גדול מהגובה הפנוי בפועל, וה-`Center` פשוט חתך את הכפתור מתחת לקו —
  בניגוד לבדיקות שנעשו בדפדפן שולחני עם viewport גבוה ובלי פרסומת טעונה,
  ששם זה תמיד נראה תקין. תוקן: המצב הריק עטוף עכשיו ב-`SingleChildScrollView`
  (גולל אם צריך, אף פעם לא חותך), והצטמצם קצת (עיגול/מרווחים קטנים יותר)
  כדי שיתאים בלי גלילה ברוב המכשירים. נבדק ב-viewport של 390×520 (קטן
  בהרבה ממכשיר אמיתי) — הכפתור נשאר גלוי במלואו.
- 🐛→✅ **פרסומות מבוטלות זמנית** — התרומה שלהן לחיסכון בגובה המסך (הבאג
  למעלה) לא הצדיקה את זה, ועדיין אין מזהי AdMob אמיתיים בכל מקרה
  (`lib/widgets/ad_banner.dart`, `_adsEnabled = false`) — דגל בודד להחזרה
  כשהעניין הזה מטופל.
- 🐛→✅ **תיבת הערה בורחת ולא פרופורציונאלית לטקסט** — תיבת הבחירה (המסגרת
  המקווקוות) תמיד תפסה את כל הרוחב המוגדר לפי `widthFraction`, גם כשהטקסט
  בפועל היה קצר בהרבה — נשארה מסגרת רחבה וריקה ברובה עם הטקסט דבוק לקצה
  אחד (בולט במיוחד בעברית, RTL). תוקן ע"י שימוש ב-`TextPainter` עם
  `maxWidth` בלבד (בלי `minWidth` שכפה את הרוחב המלא) — התיבה עכשיו
  מתאימה בדיוק לרוחב הטקסט האמיתי, וטקסט ארוך עדיין עוטף שורה כרגיל.
  התיקון הוחל זהה גם במסך (`placement_overlay.dart`) וגם בייצוא
  (`export_service.dart`), לפי עקרון ה-WYSIWYG של האפליקציה. נבדק בבדיקת
  יחידה חדשה שמוודאת שהדיו בתמונה המיוצאת נשאר בטווח צר סביב הטקסט.
- ✅ **היסטוריה כוללת כל מסמך שנפתח, לא רק כאלה שנחתמו** — פתיחת כל מסמך
  (מהבורר, שיתוף, גרירה, "פתיחה עם") יוצרת עכשיו רשומת היסטוריה מיידית,
  עוד לפני חתימה או שליחה — כך שקל למצוא מסמך גם אם לא הספיקו לסיים אותו.
  כל רשומה מסומנת בתגית "נחתם" או "נפתח בלבד" לפי המצב בפועל (אותו מסמך
  יכול להופיע פעמיים — פעם כשנפתח, ופעם כשנחתם ונשלח).

- 🐛→✅ **"הוספת דפים" בדפדפן נכשלה בשקט** (`הייצוא נכשל`) בכל פעם שהמסמך
  שכבר פתוח באתר קיבל עמודים נוספים — הדפדפן (pdf.js) "מנתק" את מאגר
  הבייטים המקורי אחרי שהוא מרנדר אותו לתצוגה, וקוד ניהול העמודים ניסה
  לקרוא מאותו מאגר שוב כדי לצרף אליו עמודים. תוקן ע"י שמירת עותק נפרד
  לרינדור, כך שהבייטים המקוריים תמיד נשארים קריאים. נתפס ותוקן רק הודות
  לבדיקה חיה בדפדפן אמיתי (Playwright) — לא נתפס בבדיקות היחידה כי הן לא
  עוברות דרך שכבת הרינדור בדפדפן.
- 🐛→✅ **גיבוי/שחזור לא כלל בפועל שום חתימה/חותמת** מאז שהמעבר לספריית
  חתימות/חותמות מרובות (סבב 9) — קוד הגיבוי עדיין קרא מהאחסון הישן
  (פריט אחד בלבד), שהתרוקן בהגירה החד-פעמית לספרייה החדשה. כל קובץ גיבוי
  שנוצר מאז היה חסר חתימות וחותמות. תוקן: הגיבוי עכשיו קורא מהספרייה
  הנוכחית במלואה. אומת קצה-לקצה: יצירת חתימה → גיבוי → ייבוא במכשיר "נקי"
  לגמרי → החתימה חוזרת נכון.
- 🐛→✅ **Application ID עדיין ברירת מחדל של Flutter** (`com.example.*`),
  ולא זהה בין אנדרואיד ל-iOS — תוקן ל-`com.rotem.quicksign` בכל הפלטפורמות
  (אנדרואיד: namespace+applicationId+חבילת Kotlin, iOS/macOS: bundle
  identifier, Linux: APPLICATION_ID). אומת: `flutter analyze` + `flutter
  build apk --release` נקיים אחרי השינוי.
- 🐛→✅ **release Android נחתם עם מפתח debug** — נוצר upload keystore אמיתי
  (נשלח לבעל המוצר בנפרד, לא בגיט), נוסף `signingConfigs["release"]`
  ב-`build.gradle.kts` שמשתמש בו כש-`android/app/keystore.jks` קיים
  ונופל חזרה ל-debug signing אחרת (כך שהפיתוח המקומי לא נשבר), ונוסף
  `.github/workflows/build-aab.yml` שמרכיב אותו מ-GitHub Secret ב-CI.

---

## 🔑 ממתין לבעל המוצר

- 🔑 **התחברות עם Google/Apple** (Firebase Auth) — **הקוד כבר מוכן ובנוי**:
  `lib/services/auth_service.dart` (Google + Apple sign-in, כולל עקיפת
  הבאג הידוע ב-Pigeon codec של `firebase_auth`), מסך ההגדרות כבר מציג
  כפתורי כניסה אמיתיים במקום "בקרוב" ברגע שיש פרויקט מוגדר, ו-
  `Firebase.initializeApp` עטוף ב-try/catch כך שהאפליקציה ממשיכה לעבוד
  רגיל גם בלי הגדרה (מה שקורה כרגע — `lib/firebase_options.dart` הוא
  placeholder). מה שנשאר, וזה הצעד היחיד שבאמת דורש אותך: ביקור אחד
  ב-console.firebase.google.com — (1) Authentication → Sign-in method
  → Google → Enable, (2) Project settings → Service accounts →
  Generate new private key (JSON) ותשלח לי אותו. מזה אני ממשיך לבד:
  `flutterfire configure`, רישום Android+iOS, SHA-1 fingerprints, ומחליף
  את ה-placeholder בקובץ האמיתי — בלי צורך בעוד login. Apple Sign-In
  דורש בנוסף חשבון Apple Developer בתשלום (נפרד, רק כשתרצה).
- 🔑 **גישת חשבון מלאה** ל-Drive / OneDrive בתוך האפליקציה — רשימת קבצים,
  ניווט, פתיחה ישירה מהחשבון (לא רק שמירה לתיקייה קבועה). דורש OAuth
  Client ID מ-Google Cloud Console + רישום Azure (OneDrive) — מפתחות
  שרק בעל החשבון יכול ליצור.
- 🔑 מעבר ל-Firebase Hosting + דומיין משלך — נשאר רק Service Account
  ו-Secrets (ההוראות ב-`.github/workflows/deploy-firebase.yml`)
- 🔑 מזהי AdMob אמיתיים (מניפסט, Info.plist, ad_banner.dart)
- 🔑 **הקמת Firebase Auth (Google+Apple) + הגשה לחנויות** — התהליך המדויק
  מתועד ב-`COWORK.md` (מבוסס על `rotem-ya/claude-guides`, נבדק בפועל
  בפרויקט קודם). כל מה שניתן להכין מראש בקוד כבר מוכן (ר' בדיקוני מהסבב
  האחרון); מה שנשאר דורש בעל החשבון: יצירת/רישום פרויקט Firebase,
  Apple Developer, Codemagic, ו-4 GitHub Secrets לחתימת ה-AAB.
- 🔑 iOS: Mac + חשבון Apple Developer + Share Extension ב-Xcode
